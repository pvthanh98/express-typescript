import {QuestionRepository} from "../src/repositories/question.repository";

(async function execute(){
    // // const questionRepo = new QuestionRepository();
    // // const question = await questionRepo.findByTitle("Question 1");
    // // console.log(question);

    
    // const rawData = await QuestionRepository.query(`SELECT * FROM question where question.title='Question 1'`)
    // console.log(rawData)


    // const questionV2 = await QuestionRepository.findOneByTitle("Question 1");
    

    // if (questionV2) {
    //     const questionId = await QuestionRepository.getId(questionV2)
    //     console.log(questionId);
    // }

    // QuestionRepository.insert({
    //     title:"Question 9",
    //     text: "Description"
    // })

})()